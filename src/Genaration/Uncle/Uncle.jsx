import React, { useContext } from 'react';
import Cusigen from '../Cusigen/Cusigen';

const Uncle = () => {
    return (
        <div>
            <h2>Uncle</h2>
            <Cusigen></Cusigen>
        </div>
    );
};

export default Uncle;